name: Spring Boot CI with Docker

on:
  push:
    branches: [ "feature/#25-ci-cd-pipeline" ]
  pull_request:
    branches: [ "feature/#25-ci-cd-pipeline" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew
        working-directory: ./dockerEx/simple-docker-git-action

      - name: Build with Gradle (skip tests)
        run: ./gradlew build -x test
        working-directory: ./dockerEx/simple-docker-git-action

#     단순 빌드 테스트트
      - name: Build Docker image
        run: docker build -t my-springboot .
        working-directory: ./dockerEx/simple-docker-git-action

 # 실제 배포 서버가 없으므로 등록/배포 단계는 주석 처리
    # - name: DockerHub 로그인 및 푸시 예시(옵션)
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKERHUB_USERNAME }}
    #     password: ${{ secrets.DOCKERHUB_TOKEN }}
    # - name: Docker 이미지 등록
    #   run: docker push my-springboot